class Solution(object):
    def lengthOfLastWord(self, s):
        """
        :type s: str
        :rtype: int
        """
        x = s.split()
        
        if len(x) > 0:
            return len(x[-1])
        
        else:
            return 0
